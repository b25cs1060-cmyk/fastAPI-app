apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    name: postgres-service
    app: fastapi
spec:
  type: ClusterIP
  selector:
    name: postgres-pod
    app: fastapi
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
